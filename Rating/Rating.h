#ifndef RATING_H
#define RATING_H
#include <bits/stdc++.h>
class Rating{
   private:
      float score;
      std::string comment;
   public:
      Rating(float i_score, std::string i_comment);
};
#endif